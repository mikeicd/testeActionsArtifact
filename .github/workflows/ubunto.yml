name: Build and Test

on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # ... other steps to build and test your code ...

    - name: Create artifact
      uses: actions/upload-artifact@v2
      with:
        name: my-artifact
        path: ./README.md